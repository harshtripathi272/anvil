# compiled binaries
/anvil
/anvil.exe

# local databases
*.anvil

# verification output (docs/benchmarks/ holds the committed evidence)
/results/

# local tooling
/.commandcode/

# video tooling (Remotion skills) — never part of the Anvil product
/.agents/
/.claude/
/skills-lock.json
